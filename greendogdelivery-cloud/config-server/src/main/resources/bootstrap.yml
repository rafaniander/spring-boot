server:
  port: 8888
spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: /home/rafael/Projetos/rafaniander/spring-boot/greendogdelivery-cloud/config-repo